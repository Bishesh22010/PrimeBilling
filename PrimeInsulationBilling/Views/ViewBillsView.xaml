<UserControl x:Class="PrimeInsulationBilling.Views.ViewBillsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="650"
             Background="#F4F7FC" Loaded="UserControl_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="View Generated Bills" Grid.Row="0"
                   FontSize="32" FontWeight="SemiBold" Foreground="#1A3C6E" Margin="30,0,30,20"/>

        <!-- Toolbar -->
        <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="20,10">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="RefreshButton" Content="Refresh List" Background="#007ACC" Foreground="White" 
                        FontSize="14" Padding="15,8" BorderThickness="0" Click="RefreshButton_Click"/>

                <Button x:Name="OpenButton" Content="Open Selected Bill" Background="#28a745" Foreground="White" 
                        FontSize="14" Padding="15,8" BorderThickness="0" Margin="10,0,0,0" Click="OpenButton_Click"/>

                <TextBlock x:Name="lblBillCount" Text="Total Bills: 0" VerticalAlignment="Center" 
                           FontSize="16" FontWeight="SemiBold" Foreground="#666666" Margin="20,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Bill List -->
        <ListView x:Name="BillsListView" Grid.Row="2" Margin="20"
                  FontSize="14" BorderBrush="#E0E0E0"
                  MouseDoubleClick="BillsListView_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Bill Filename" Width="350" DisplayMemberBinding="{Binding FileName}"/>
                    <GridViewColumn Header="Date Created" Width="200" DisplayMemberBinding="{Binding DateCreated}"/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</UserControl>
